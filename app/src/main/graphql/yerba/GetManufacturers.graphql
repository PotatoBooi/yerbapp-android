query GetManufacturers( $perPage: Int, $orderBy: String, $order: String) {
    manufacturers(perPage: $perPage, orderBy: $orderBy, order: $order){
        items{
            id,
            name,
            country
        }
    }
}
