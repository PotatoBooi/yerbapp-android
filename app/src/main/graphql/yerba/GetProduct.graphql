query GetProduct( $productId:  ID!){
    product(productId: $productId){
        name,
        details,
        photoUrl,
        manufacturer{
            name,
            country
        },
        type{
            name
        }
    }
}
