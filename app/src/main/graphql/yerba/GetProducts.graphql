
query GetProducts ( $perPage: Int, $offset: Int, $orderBy: String){
    products(perPage: $perPage, offset: $offset, orderBy: $orderBy )  {
        items{
            id,
            name,
            photoUrl,
            overallAverage
        }
    }
}

