
query GetProducts ($searchByName: String, $perPage: Int, $offset: Int, $orderBy: String){
    products(searchByName: $searchByName, perPage: $perPage, offset: $offset, orderBy: $orderBy )  {
        items{
            id,
            name,
            photoUrl,
            overallAverage
        }
    }
}

