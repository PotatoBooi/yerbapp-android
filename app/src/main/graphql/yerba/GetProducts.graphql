query GetProducts ($personalizeForUer: ID, $searchByName: String, $perPage: Int, $offset: Int, $orderBy: String){
    products(personalizeForUser: $personalizeForUer, searchByName: $searchByName, perPage: $perPage, offset: $offset, orderBy: $orderBy )  {
        items{
            id,
            name,
            photoUrl,
            overallAverage
        }
    }
}

